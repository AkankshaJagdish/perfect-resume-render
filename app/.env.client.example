# All client-side env vars must start with REACT_APP_.
# Render sets this from the server service URL in render.yaml.
REACT_APP_API_URL=http://localhost:3001

# Google Analytics measurement ID used after cookie consent.
REACT_APP_GOOGLE_ANALYTICS_ID=G-...
